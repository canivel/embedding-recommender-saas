# Example Terraform variables file
# Copy this to terraform.tfvars and fill in your values

aws_region  = "us-west-2"
environment = "dev"

# PostgreSQL credentials
postgres_username = "postgres"
postgres_password = "CHANGE_ME_IN_PRODUCTION"  # Use AWS Secrets Manager in production

# For production, consider:
# - Using larger instance types
# - Enabling multi-AZ deployments
# - Increasing backup retention
# - Using dedicated node groups for different workload types
